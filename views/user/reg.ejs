<% include ../compents/head.ejs %>
    <header>
        <h1>用户注册</h1>
    </header>
    <div class="container">
    <form class="form-horizontal" style="padding-top:50px;">
      <div class="form-group">
        <label class="col-sm-2 control-label">用户名：</label>
        <div class="col-sm-10">
          <input type="text" class="form-control" id="name" placeholder="用户名" value="" >
        </div>
      </div>
      <div class="form-group">
        <label class="col-sm-2 control-label">密码：</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" id="password" placeholder="密码" value="" >
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" data-loading-text="Loading..." class="btn btn-default">注册</button>
        </div>
      </div>
    </form>
    </div>
<script>
$(function(){
    let _from = $('.form-horizontal');
    const _id = _from.data('id');
    const _btn = _from.find('button');
    _btn.on('click',function(e){
      const $btn = $(this).button('loading');
      e.preventDefault();
    $.ajax({
        url: "/user/doReg",
        type: "POST",
        data:{
          name:$('#name').val(),
          password:$('#password').val()
        }
    }).done(function(data) {
      $btn.button('reset');
      console.log(data)
      window.location.href='http://localhost:8088/'
        
    }).fail(function(jqXHR, textStatus) {
      $btn.button('reset')
      alert( "Request failed: " + textStatus );
    });;
  })
})
</script>
<% include ../compents/foot.ejs %>